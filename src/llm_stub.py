"""
LLM Stub Module

This file provides a placeholder function for calling an LLM.
Users can replace this with actual API calls (OpenAI, Google, Anthropic, etc.)
without changing the structure of the rest of the project.
"""

def call_llm(prompt: str) -> str:
    """
    Placeholder LLM call.
    Replace this with your actual API implementation.

    Example:
        from openai import OpenAI
        client = OpenAI()
        response = client.responses.create(...)
    """
    raise NotImplementedError(
        "LLM call not implemented. Insert your API client here."
    )
